enum WebRTCMessageTypes {
  ANSWER = 'answer',
  CANDIDATE = 'candidate',
  CONNECT = 'connect',
  CONNECT_ERROR = 'connect_error',
  CREATED = 'created',
  DISCONNECT = 'disconnect',
  ERROR = 'error',
  FULL = 'full',
  JOINED = 'joined',
  OFFER = 'offer',
  READY = 'ready',
}

export default WebRTCMessageTypes;
